# ScheduleFlow — Mobile UX (Next.js)

Routes
- /schedule (calendar/list)

Mobile pattern
- Default to agenda list; calendar as compact month picker overlay.
- Filters in mobile sheet (date range, owner, status).

Acceptance
- No tiny calendar taps; all filter controls ≥ 44px.
